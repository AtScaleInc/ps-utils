{
  "name": "operation-cli",
  "version": "0.1.0",
  "description": "CLI framework with operations and required parameters",
  "type": "module",
  "bin": {
    "operation-cli": "dist/cli.js"
  },
  "files": [
    "dist",
    "action.yml"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run generate:completions",
    "start": "npm run build && node dist/cli.js",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "generate:completions": "node dist/scripts/generate-completions.js"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "ejs": "^3.1.10",
    "java": "^0.18.0",
    "jdbc": "^0.7.6",
    "js-yaml": "^4.1.1",
    "xml2js": "^0.6.2",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.3.0",
    "@types/xml2js": "^0.4.14",
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
