{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://atscale.com/schemas/sml/model.schema.json",
  "title": "SML Model",
  "description": "Generated from resources/sml-reference/model.md — do not edit by hand.\nRun `npm run generate:sml-schema` to regenerate; see resources/sml-reference/UPSTREAM.md\nfor the pinned specification revision.\n\n`additionalProperties` is true throughout: the specification prose lags the\nimplementation, so a closed schema would reject valid files. Known gaps: `visualize_in_bi_tool` (hierarchy level references, emitted by src/algorithm/sml-serializer.ts); `is_unique_key` (secondary attributes, emitted by src/algorithm/sml-serializer.ts. The reference documents this key on LevelAttribute and SharedDegenerateColumns but not here, which is why the allowlist is scoped by class rather than by key).",
  "type": "object",
  "properties": {
    "unique_name": {
      "description": "The unique name of the model. This must be unique across all repositories and subrepositories.",
      "type": "string"
    },
    "object_type": {
      "description": "The type of object defined by the file. For models, the value of this property should be `model`.",
      "const": "model"
    },
    "label": {
      "description": "The name of the model, as it appears in the consunmption tool. This value does not need to be unique.",
      "type": "string"
    },
    "relationships": {
      "description": "Defines the relationships between the model's fact datasets and first order dimensions. These are called fact relationships. **Note:** These relationships are separate from those defined at the dimension level: relationships at the model level involve fact datasets, while those at the dimension level do not. **Note:** Degenerate dimensions have relationships to the fact datasets on which they are based. However, these dimensions do not need a `relationships` property as they are created by referencing the fact dataset columns directly. If you do not want to add relationships to the model, the value of this property must be `[]`. For example: `relationships: []`",
      "items": {
        "$ref": "#/$defs/Relationship"
      },
      "type": "array"
    },
    "metrics": {
      "description": "A list of references to metrics and calculations used in the model.",
      "items": {
        "$ref": "#/$defs/MetricReference"
      },
      "type": "array"
    },
    "description": {
      "description": "A description of the model.",
      "type": "string"
    },
    "dimensions": {
      "description": "A list of references to degenerate dimensions defined on a specific fact dataset in the model.",
      "items": {
        "type": "string"
      },
      "type": "array",
      "x-sml-ref": {
        "kind": "dimension"
      }
    },
    "perspectives": {
      "description": "Perspectives are deployable subsets of the data model. They are meant to make it easier for analysts to query only the subset of data that is relevant to their purposes or responsibilities. Rather than provide analysts with the entire data model, you can make specific dimensions, hierarchies, levels, secondary attributes, measures, and calculated measures invisible to them. **Note:** We recommend that you add perspectives _after_ a model has been fully tested. Although you can edit a model after adding perspectives, any changes might require you to update the perspectives to hide new objects that would otherwise be visible to all users. The semantic engine imposes no limit on the number of perspectives that you can add to a model. Perspectives contain no data themselves, but are simply virtual views of the data.",
      "items": {
        "$ref": "#/$defs/Perspective"
      },
      "type": "array"
    },
    "drillthroughs": {
      "description": "In BI tools, a drillthrough enables you to view detailed information about a specific cell within a visualization as needed. This provides an alternative to including lots of fine-grained attributes in large pivot tables, which can result in performance issues. Moving these attributes to drillthroughs means they are only returned if a user requests them for a specific cell, rather than for the entire table. In an SML model, you can define drillthroughs that include the specific level of detail to return for these types of queries.",
      "items": {
        "$ref": "#/$defs/Drillthrough"
      },
      "type": "array"
    },
    "aggregates": {
      "description": "The `aggregates` property in a model file enables you to add user-defined aggregates (UDAs). In general, we recommend relying on the aggregate tables automatically generated by the semantic engine. However, there are cases that are not covered by system-defined aggregates. For example: **Metrics on dimensions:** The semantic engine does not generate aggregate tables for metrics that are local to a dimension only (a secondary metrical attribute in the model). **Non-additive metrics:** The semantic engine does not generate aggregate tables for non-additive metrics, which are useful for distinct counts. This is because such an aggregate table defined for one query would not be usable by other queries. If you require aggregate tables that contain these types of dimensional attributes or metrics, you should define your own manually using the `aggregates` property.",
      "items": {
        "$ref": "#/$defs/Aggregate"
      },
      "type": "array"
    },
    "partitions": {
      "description": "The `partitions` property in a model file enables you to create prioritized partitioning hints that the semantic engine uses to create partitioned aggregate tables. The actual partitioning scheme used by the engine depends on a number of factors, including: Whether the aggregate includes a column that matches a partition hint. Whether the semantic engine statistics suggest that partitioning would be worthwhile. Whether the target data warehouse supports table partitioning. Within SML, all partitions used in a model are defined in the model file itself.",
      "items": {
        "$ref": "#/$defs/Partition"
      },
      "type": "array"
    },
    "dataset_properties": {
      "description": "Defines dataset properties that are specific to the model, rather than the repository.",
      "$ref": "#/$defs/ModelDatasetProperties"
    },
    "overrides": {
      "description": "The `overrides` property in a model file enables the creation of query name overrides for metrics and degenerate dimensions referenced in а model. This scenario arises from legacy projects where metrics or dimensions in different models can use the same `unique_name` when deployed. Since project scope uniqueness is enforced these metrics/dimensions are required have a different `unique_name` value in the repo. When a model is deployed the overridden `unique_name` is replaced by the original value. The best practice is to never use the same `unique_name` for different objects across models so overrides should only be used when migrating from a legacy model and wanting to maintain the same query name for existing interfaces. **Note:** This applies only for degenerate dimensions, NOT the dimensions part of relationships. The object key must be a metric or a dimension referenced in the model.",
      "$ref": "#/$defs/ModelOverrides"
    },
    "include_default_drillthrough": {
      "description": "_Not described in the SML reference prose; adopted from the specification's own entity-relationship diagram._",
      "type": "boolean"
    }
  },
  "required": [
    "unique_name",
    "object_type",
    "label",
    "relationships",
    "metrics"
  ],
  "additionalProperties": true,
  "$defs": {
    "MetricReference": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the metric or calculation.",
          "type": "string",
          "x-sml-ref": {
            "kind": "metric_or_calculation"
          }
        },
        "folder": {
          "description": "The name of the folder in which the metric/calculation is displayed in BI tools. If your model has a lot of metrics/calculations, folders are a good way to organize them.",
          "type": "string"
        }
      },
      "required": [
        "unique_name"
      ],
      "additionalProperties": true
    },
    "ModelDatasetProperties": {
      "type": "object",
      "properties": {
        "allow_aggregates": {
          "description": "Enables the semantic engine to create aggregates for datasets in the repository.",
          "type": "boolean"
        },
        "allow_local_aggs": {
          "description": "Enables local aggregation for datasets in the repository.",
          "type": "boolean"
        },
        "allow_peer_aggs": {
          "description": "Enables aggregation on data derived from datasets in data warehouses that are different from the source dataset.",
          "type": "boolean"
        },
        "allow_preferred_aggs": {
          "description": "Allow aggregates to be built in preferred storage.",
          "type": "boolean"
        },
        "create_hinted_aggregate": {
          "description": "Enables the creation of hinted aggregates for the dataset.",
          "type": "boolean"
        }
      },
      "additionalProperties": true
    },
    "From": {
      "type": "object",
      "properties": {
        "dataset": {
          "description": "The physical fact dataset you want to link to a dimension.",
          "type": "string",
          "x-sml-ref": {
            "kind": "dataset"
          }
        },
        "join_columns": {
          "description": "The columns within the `dataset` that you want to use as join columns.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "column",
            "in": {
              "dataset": "0/dataset"
            }
          }
        }
      },
      "required": [
        "dataset",
        "join_columns"
      ],
      "additionalProperties": true
    },
    "To": {
      "type": "object",
      "properties": {
        "dimension": {
          "description": "The name of the dimension to which the `from` dataset is joined.\n\n**Required:** required if `row_security` is undefined",
          "type": "string",
          "x-sml-ref": {
            "kind": "dimension"
          }
        },
        "level": {
          "description": "The `unique_name` of the level attribute within the `dimension` to use for the relationship.\n\n**Required:** required if `row_security` is undefined",
          "type": "string",
          "x-sml-ref": {
            "kind": "level",
            "in": {
              "dimension": "0/dimension"
            }
          }
        },
        "row_security": {
          "description": "For security relationships, the row security object that the `from` dataset is joined to.\n\n**Required:** required if `dimension` and `level` are undefined",
          "type": "string",
          "x-sml-ref": {
            "kind": "row_security"
          }
        }
      },
      "additionalProperties": true
    },
    "ConstraintTranslation": {
      "type": "object",
      "properties": {
        "level": {
          "description": "Indicates the dimension level to which the constraint translation applies.",
          "type": "string",
          "x-sml-ref": {
            "kind": "level"
          }
        },
        "from_columns": {
          "description": "Lists the column(s) in the dataset that should be used for the join.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "column",
            "in": {
              "dataset": "2/from/dataset"
            }
          }
        }
      },
      "required": [
        "level",
        "from_columns"
      ],
      "additionalProperties": true
    },
    "Relationship": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the relationship. This must be unique within the model file.",
          "type": "string"
        },
        "from": {
          "description": "Defines the side of the relationship that contains the physical fact dataset. Typically, this is a join column in the fact dataset.",
          "$ref": "#/$defs/From"
        },
        "to": {
          "description": "Defines the dimension that the `from` dataset is linked to.",
          "$ref": "#/$defs/To"
        },
        "role_play": {
          "description": "For role-playing relationships only. Defines the role-playing template for the relationship. The role-playing template is the prefix and/or suffix that is added to every attribute in the role-played dimension. This value must be in one of the following formats (including quotation marks): **Prefix:** `\"<prefix> {0}\"` **Suffix:** `\"{0} <suffix>\"` **Prefix and suffix:** `\"<prefix> {0} <suffix>\"` For example, if you wanted to use the prefix **Order**, you would set `role_play` to `\"Order {0}\"`.",
          "type": "string"
        },
        "constraint_translation": {
          "description": "Defines the translation of dimension filter constraints into fact table partition column constraints. This can significantly improve query performance for cases where fact-based aggregates are not used.",
          "$ref": "#/$defs/ConstraintTranslation"
        },
        "type": {
          "description": "_Not described in the SML reference prose; adopted from the specification's own entity-relationship diagram._",
          "type": "string"
        }
      },
      "required": [
        "unique_name",
        "from",
        "to"
      ],
      "additionalProperties": true
    },
    "PerspectiveDimension": {
      "type": "object",
      "properties": {
        "name": {
          "description": "The name of the dimension to be hidden in the perspective.",
          "type": "string",
          "x-sml-ref": {
            "kind": "dimension"
          }
        },
        "hierarchies": {
          "description": "A list of the specific hierarchies within the dimension to hide in the perspective. Supported properties:",
          "items": {
            "$ref": "#/$defs/PerspectiveHierarchy"
          },
          "type": "array"
        },
        "secondary_attributes": {
          "description": "A list of the dimension's secondary attributes to hide in the perspective.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "secondary_attribute",
            "in": {
              "dimension": "0/name"
            }
          }
        },
        "relationships_path": {
          "description": "A list of relationships used to specify role-playing.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "relationship"
          }
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": true
    },
    "PerspectiveHierarchy": {
      "type": "object",
      "properties": {
        "name": {
          "description": "The name of the hierarchy.",
          "type": "string",
          "x-sml-ref": {
            "kind": "hierarchy",
            "in": {
              "dimension": "1/name"
            }
          }
        },
        "level": {
          "description": "Defines a single level in the hierarchy to be hidden in the perspective. All levels below the specified level will also be hidden.",
          "type": "string",
          "x-sml-ref": {
            "kind": "level",
            "in": {
              "hierarchy": "0/name"
            }
          }
        },
        "levels": {
          "description": "⚠️ **DEPRECATED** use `level` instead.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "level",
            "in": {
              "hierarchy": "0/name"
            }
          }
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": true
    },
    "Perspective": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the perspective. This must be unique within the model file.",
          "type": "string"
        },
        "metrics": {
          "description": "A list of the specific metrics and calculations to be hidden in the perspective.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "metric_or_calculation"
          }
        },
        "dimensions": {
          "description": "A list of the specific dimensions and their hierarchies to be hidden in the perspective. By default, all objects within a dimension are visible. The lowest granularity objects specified are hidden and the objects above it are not. Hiding a level in a hierarchy hides all levels below it. Hiding a hierarchy hides all levels in it. Hiding a dimension hides all objects within it including hierarchies and secondary attributes. If a dimension is not hidden, secondary attributes can be hidden individually.",
          "items": {
            "$ref": "#/$defs/PerspectiveDimension"
          },
          "type": "array"
        }
      },
      "required": [
        "unique_name"
      ],
      "additionalProperties": true
    },
    "AttributeReferenceDrillthrough": {
      "type": "object",
      "properties": {
        "name": {
          "description": "The name of the attribute to include in the drillthrough.",
          "type": "string",
          "x-sml-ref": {
            "kind": "attribute",
            "in": {
              "dimension": "0/dimension"
            }
          }
        },
        "dimension": {
          "description": "The dimension that the attribute defined by `name` appears in.",
          "type": "string",
          "x-sml-ref": {
            "kind": "dimension"
          }
        },
        "relationships_path": {
          "description": "A list of relationships path.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "relationship"
          }
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": true
    },
    "Drillthrough": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the drillthrough. This must be unique within the model file.",
          "type": "string"
        },
        "metrics": {
          "description": "A list of the metrics to include in the drillthrough.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "metric_or_calculation"
          }
        },
        "notes": {
          "description": "Notes about the drillthrough.",
          "type": "string"
        },
        "attributes": {
          "description": "A list of the specific attributes to include in the drillthrough.",
          "items": {
            "$ref": "#/$defs/AttributeReferenceDrillthrough"
          },
          "type": "array"
        }
      },
      "required": [
        "unique_name",
        "metrics"
      ],
      "additionalProperties": true
    },
    "AttributeReference": {
      "type": "object",
      "properties": {
        "name": {
          "description": "The name of the dimension attribute to include. These values are used to group the summarized metric data in the resulting aggregate table. Note that user-defined aggregate definitions are fixed: they do not include every level of a hierarchy unless they are explicitly defined.\n\n**Required:** required if `row_security` is undefined",
          "type": "string",
          "x-sml-ref": {
            "kind": "attribute",
            "in": {
              "dimension": "0/dimension"
            }
          }
        },
        "dimension": {
          "description": "The dimension to which the attribute defined by `name` belongs.\n\n**Required:** required if `row_security` is undefined",
          "type": "string",
          "x-sml-ref": {
            "kind": "dimension"
          }
        },
        "row_security": {
          "description": "Allows row security to be included in a user defined aggregate.\n\n**Required:** required if `dimension` and `name` are undefined",
          "type": "string",
          "x-sml-ref": {
            "kind": "row_security"
          }
        },
        "partition": {
          "description": "Adds a partition to the aggregate, and determines whether it should be defined on the key column, name column, or both. Supported values: `name`, `key`, `name+key`",
          "type": "string"
        },
        "distribution": {
          "description": "The distribution keys to use when creating the aggregate table. If your aggregate data warehouse supports distribution keys, then the semantic engine uses the specified keys when creating the aggregate table. By default, the order of distribution keys follows the left-to-right order of the attributes in the `attributes` array. Use `distribution_rank` to control distribution key order independently of the partition key order.",
          "type": "string"
        },
        "partition_rank": {
          "description": "An integer between 1 and 128 that explicitly controls the order of partition keys, independently of the distribution key order. Lower values indicate higher priority. When set, `partition_rank` overrides the default left-to-right attribute order for partitioning. Rank values across all attributes that define a `partition` in the same aggregate must form a consecutive sequence starting from 1 (e.g. `1, 2, 3`).",
          "type": "integer"
        },
        "distribution_rank": {
          "description": "An integer between 1 and 128 that explicitly controls the order of distribution keys, independently of the partition key order. Lower values indicate higher priority. When set, `distribution_rank` overrides the default left-to-right attribute order for distributions. Rank values across all attributes that define a `distribution` in the same aggregate must form a consecutive sequence starting from 1 (e.g. `1, 2, 3`).",
          "type": "integer"
        },
        "relationships_path": {
          "description": "_Not described in the SML reference prose; adopted from the specification's own entity-relationship diagram._",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "relationship"
          }
        }
      },
      "additionalProperties": true
    },
    "Aggregate": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the aggregate. This must be unique within the model file. Aggregate table names used by the query engine are system-generated, but they include the first 14 characters of the user-supplied name at the end of the internal ID name. This name can help you identify when a user-defined aggregate is used in a query. For example: `as_agg_internal-id_my-uda-name`",
          "type": "string"
        },
        "label": {
          "description": "The name of the aggregate, as it appears in the consunmption tool. This value does not need to be unique.",
          "type": "string"
        },
        "caching": {
          "description": "This setting will control whether the aggregate is pinned in local cache.",
          "type": "string",
          "enum": [
            "engine-memory"
          ]
        },
        "metrics": {
          "description": "A list of the metrics and calculations to include in the aggregate definition. This is the data that is summarized in the resulting aggregate table.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "metric_or_calculation"
          }
        },
        "attributes": {
          "description": "A list of the dimension attributes to include in the aggregate definition.",
          "items": {
            "$ref": "#/$defs/AttributeReference"
          },
          "type": "array"
        }
      },
      "required": [
        "unique_name",
        "label",
        "metrics"
      ],
      "additionalProperties": true
    },
    "Partition": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the partition. This must be unique within the model file.",
          "type": "string"
        },
        "dimension": {
          "description": "The dimension that contains the `attribute` the partition is based on.",
          "type": "string",
          "x-sml-ref": {
            "kind": "dimension"
          }
        },
        "attribute": {
          "description": "The attribute that the partition is based on.",
          "type": "string",
          "x-sml-ref": {
            "kind": "attribute",
            "in": {
              "dimension": "0/dimension"
            }
          }
        },
        "type": {
          "description": "Determines whether the partition is defined on the name column, key column, or both.",
          "type": "string",
          "enum": [
            "name",
            "key",
            "name+key"
          ]
        }
      },
      "required": [
        "unique_name",
        "dimension",
        "attribute",
        "type"
      ],
      "additionalProperties": true
    },
    "ModelOverrides": {
      "type": "object",
      "properties": {
        "query_name": {
          "description": "The query name that the metric or dimension should be resolved to. Sample `overrides`:",
          "type": "string",
          "x-sml-ref": {
            "kind": "metric_or_dimension"
          }
        }
      },
      "required": [
        "query_name"
      ],
      "additionalProperties": true
    }
  }
}
