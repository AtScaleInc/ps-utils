{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://atscale.com/schemas/sml/composite-model.schema.json",
  "title": "SML Composite Model",
  "description": "Generated from resources/sml-reference/composite-model.md — do not edit by hand.\nRun `npm run generate:sml-schema` to regenerate; see resources/sml-reference/UPSTREAM.md\nfor the pinned specification revision.\n\n`additionalProperties` is true throughout: the specification prose lags the\nimplementation, so a closed schema would reject valid files. Known gaps: `visualize_in_bi_tool` (hierarchy level references, emitted by src/algorithm/sml-serializer.ts); `is_unique_key` (secondary attributes, emitted by src/algorithm/sml-serializer.ts. The reference documents this key on LevelAttribute and SharedDegenerateColumns but not here, which is why the allowlist is scoped by class rather than by key).",
  "type": "object",
  "properties": {
    "unique_name": {
      "description": "The unique name of the composite model. This must be unique across all repositories and subrepositories.",
      "type": "string"
    },
    "object_type": {
      "description": "The type of object defined by the file. For composite models, this must be `composite_model`.",
      "const": "composite_model"
    },
    "label": {
      "description": "The name of the composite model as it appears in the consumption tool. This value does not need to be unique.",
      "type": "string"
    },
    "description": {
      "description": "A description of the composite model.",
      "type": "string"
    },
    "models": {
      "description": "A list of the models that make up the composite model. These must meet the following criteria: They cannot be other composite models. They must all have at least one dimension in common.",
      "items": {
        "$ref": "#/$defs/ModelReference"
      },
      "type": "array"
    },
    "metrics": {
      "description": "A list of the calculations to include in the composite model. These must meet the following criteria: They must be of object type `metric_calc`. Each calculation’s MDX formula can only contain references to objects that appear in the referenced models.",
      "items": {
        "$ref": "#/$defs/MetricReference"
      },
      "type": "array"
    },
    "aggregates": {
      "description": "> [!WARNING] > Any UDA's defined on the models referenced by the composite will not be used. They will not be automatically added The `aggregates` property in a composite model file enables you to add user-defined aggregates (UDAs).They include metrics or attributes from different models within the composite model. In general, we recommend relying on the aggregate tables automatically generated by the semantic engine. However, there are cases that are not covered by system-defined aggregates. For example: **Metrics on dimensions:** The semantic engine does not generate aggregate tables for metrics that are local to a dimension only (a secondary metrical attribute in the composite model). **Non-additive metrics:** The semantic engine does not generate aggregate tables for non-additive metrics, which are useful for distinct counts. This is because such an aggregate table defined for one query would not be usable by other queries. If you require aggregate tables that contain these types of dimensional attributes or metrics, you should define your own manually using the `aggregates` property.\n\n**Added in:** v1.4",
      "items": {
        "$ref": "#/$defs/Aggregate"
      },
      "type": "array"
    }
  },
  "required": [
    "unique_name",
    "object_type",
    "label",
    "models"
  ],
  "additionalProperties": true,
  "$defs": {
    "MetricReference": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the calculation.",
          "type": "string",
          "x-sml-ref": {
            "kind": "metric_calc"
          }
        },
        "folder": {
          "description": "The name of the folder in which this calculation appears in BI tools.",
          "type": "string"
        }
      },
      "required": [
        "unique_name"
      ],
      "additionalProperties": true
    },
    "AggregateAttributes": {
      "type": "object",
      "properties": {
        "name": {
          "description": "The name of the dimension attribute to include. These values are used to group the summarized metric data in the resulting aggregate table. Note that user-defined aggregate definitions are fixed: they do not include every level of a hierarchy unless they are explicitly defined.",
          "type": "string",
          "x-sml-ref": {
            "kind": "attribute",
            "in": {
              "dimension": "0/dimension"
            }
          }
        },
        "dimension": {
          "description": "The dimension to which the attribute defined by `name` belongs.",
          "type": "string",
          "x-sml-ref": {
            "kind": "dimension"
          }
        },
        "partition": {
          "description": "Adds a partition to the aggregate, and determines whether it should be defined on the key column, name column, or both. Supported values: `name`, `key`, `name+key`",
          "type": "string"
        },
        "distribution": {
          "description": "The distribution keys to use when creating the aggregate table. If your aggregate data warehouse supports distribution keys, then the semantic engine uses the specified keys when creating the aggregate table.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "dimension"
      ],
      "additionalProperties": true
    },
    "Aggregate": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "The unique name of the aggregate. This must be unique within the composite model file. Aggregate table names used by the query engine are system-generated, but they include the first 14 characters of the user-supplied name at the end of the internal ID name. This name can help you identify when a user-defined aggregate is used in a query. For example: `as_agg_internal-id_my-uda-name`",
          "type": "string"
        },
        "label": {
          "description": "The name of the aggregate, as it appears in the consunmption tool. This value does not need to be unique.",
          "type": "string"
        },
        "caching": {
          "description": "This setting will control whether the aggregate is pinned in local cache.",
          "type": "string",
          "enum": [
            "engine-memory"
          ]
        },
        "metrics": {
          "description": "A list of the metrics and calculations to include in the aggregate definition. This is the data that is summarized in the resulting aggregate table.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-sml-ref": {
            "kind": "metric_or_calculation"
          }
        },
        "attributes": {
          "description": "A list of the dimension attributes to include in the aggregate definition.",
          "items": {
            "$ref": "#/$defs/AggregateAttributes"
          },
          "type": "array"
        }
      },
      "required": [
        "unique_name",
        "label",
        "metrics"
      ],
      "additionalProperties": true
    },
    "ModelReference": {
      "type": "object",
      "properties": {
        "unique_name": {
          "description": "_Not described in the SML reference prose; adopted from the specification's own entity-relationship diagram._",
          "type": "string",
          "x-sml-ref": {
            "kind": "model"
          }
        }
      },
      "additionalProperties": true
    }
  }
}
